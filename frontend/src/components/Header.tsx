import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="header">
      <div className="logo">
        <span className="gradient-text">StreamVault</span>
      </div>
    </header>
  );
};

export default Header;
